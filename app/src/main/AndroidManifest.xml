<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.setupbuilder">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.SettingsActivity" />
        <activity
            android:name=".view.RegisterActivity"
            android:theme="@style/AppLightTheme" />
        <activity android:name=".view.MenuActivity" />
        <activity android:name=".view.NewSetupActivity" />
        <activity android:name=".view.ViewSetupActivity" />
        <activity android:name=".view.PasswordRecoveryActivity" />
        <activity android:name=".view.ViewPartActivity" />
        <activity android:name=".view.ListProductActivity" />
        <activity android:name=".view.ViewProductActivity" />
    </application>

</manifest>